<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Nurse</title>

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

<style>
body{font-family:'Segoe UI';background:#f4f6f9;margin:0;}
header{background:#2c3e50;color:white;padding:15px;text-align:center;}
.day{background:#eef4fa;margin:10px;padding:10px;border-radius:10px;}
.item{background:white;margin:5px;padding:10px;cursor:pointer;border-left:5px solid #3498db;}
.modal{display:none;position:fixed;top:20%;left:30%;background:white;padding:20px;border-radius:10px;}
</style>
</head>

<body>

<header>
<h1>Nurse Schedule</h1>
<a href="index.html">Home</a>
<a href="doctor.html">Doctor</a>
</header>

<div id="calendar"></div>

<div id="modal" class="modal">
<div id="details"></div>
<button onclick="closeModal()">Close</button>
</div>

<script>
const SUPABASE_URL="https://xphczwydrcuvqsuthiii.supabase.co";
const SUPABASE_KEY="YOUR_ANON_KEY_HERE";
const db=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);

async function load(){

const {data}=await db.from("appointments").select("*");

data.sort((a,b)=>new Date(a.date+" "+a.time)-new Date(b.date+" "+b.time));

let grouped={};
data.forEach(a=>{
if(!grouped[a.date]) grouped[a.date]=[];
grouped[a.date].push(a);
});

let cal=document.getElementById("calendar");
cal.innerHTML="";

for(let day in grouped){

let div=document.createElement("div");
div.className="day";
div.innerHTML="<h3>"+day+"</h3>";

grouped[day].forEach(a=>{
let item=document.createElement("div");
item.className="item";
item.innerText=a.time+" - "+a.name;
item.onclick=()=>show(a);
div.appendChild(item);
});

cal.appendChild(div);
}
}

function show(a){
document.getElementById("details").innerHTML=`
<b>Name:</b> ${a.name}<br>
<b>DOB:</b> ${a.dob}<br>
<b>Email:</b> ${a.email}<br>
<b>Date:</b> ${a.date}<br>
<b>Time:</b> ${a.time}<br>
<b>Doctor:</b> ${a.doctor}<br>
<b>Notes:</b> ${a.notes||""}
`;
document.getElementById("modal").style.display="block";
}

function closeModal(){
document.getElementById("modal").style.display="none";
}

load();
</script>

</body>
</html>
